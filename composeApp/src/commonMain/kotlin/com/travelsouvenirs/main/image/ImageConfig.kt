package com.travelsouvenirs.main.image

/** Maximum pixel length of the longest side when saving a souvenir photo. */
const val IMAGE_MAX_SIDE_PX = 1500

/** JPEG compression quality (0–100 on Android, 0.0–1.0 on iOS). */
const val IMAGE_JPEG_QUALITY = 80
